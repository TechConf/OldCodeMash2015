plugin.name=WLS12
plugin.version=12.1.2
plugin.description=Examines the configuration of an installed WebLogic Server \
domain to determine what needs to be captured during packaging and what \
configuration needs to be changed at deployment.
ovabsdk.version=3.0.0
product.version.min=11.1.1.2.0
product.version.max=12.1.2.0.0

# The 'plugin.enabled' property is only needed here because 
# plugins shipping with OVAB itself are put into the AB_INSTANCE
# template manually instead of through plugin install commands.
plugin.enabled=true
